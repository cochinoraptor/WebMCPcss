/**
 * Versión única del paquete, compartida por el CLI, el servidor MCP y los
 * exportadores (plugins generados). Debe coincidir con `package.json`
 * (hay un test que lo comprueba).
 */
export const VERSION = '0.9.0';
